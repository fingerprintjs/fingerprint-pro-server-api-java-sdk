---
"fingerprint-pro-server-api-java-sdk": minor
---

Sync OpenAPI schema to v3.7.1. Add the `RequestReadTimeout` value to `ErrorCode`, mark `Labels.label` as required, clarify `reverse` and `VisitorsGetResponse` documentation (visits are limited to one item), and update descriptions for `ProxyDetails.proxyType`, error responses, and other fields
